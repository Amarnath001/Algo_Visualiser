# Visualization Controls

This directory contains UI controls for algorithm visualization.

## Components

- `PlaybackControls` - Play, pause, stop, reset buttons
- `SpeedControl` - Slider to adjust animation speed
- `StepNavigator` - Navigate to specific algorithm steps
- `ParameterControls` - Input forms for algorithm parameters
- `StatsDisplay` - Show execution statistics
